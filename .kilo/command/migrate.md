# Shared PostgreSQL Migrations

Load and follow the `database-migrations` skill. Read `packages/db/AGENTS.md` for shared PostgreSQL and Drizzle invariants.

This command does not govern Durable Object SQLite or Wrangler migrations; follow the owning service guidance and relevant Durable Objects or Workers skill for those systems.
